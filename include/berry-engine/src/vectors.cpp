#include <berry-engine/vectors.h>

berry::Vec2::Vec2(longlong x, longlong y) {
    this->x = x;
    this->y = y;
}

berry::Vec3::Vec3(longlong x, longlong y, longlong z) {
    this->x = x;
    this->y = y;
    this->z = z;
}
